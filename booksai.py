import os
from google import genai
from google.genai.types import GenerateContentConfig, Content, Part

# جلب مفتاح Gemini من متغير البيئة
API_KEY = os.getenv("GEMINI_API_KEY")

# تهيئة العميل
client = genai.Client(api_key=API_KEY)

# 🧠 البحث الذكي عن كتاب من خلال الوصف أو الكلمات المفتاحية
def ai_search(description: str) -> str:
    """
    يقوم هذا الذكاء الاصطناعي بتحليل وصف المستخدم أو فكرته عن الكتاب
    ثم يحاول اقتراح كتاب أو أكثر من قاعدة البيانات المفترضة بناءً على المعنى.
    """
    prompt = f"""
    أنت مساعد ذكي في مكتبة إلكترونية.
    المستخدم كتب وصفًا عن كتاب يبحث عنه:
    "{description}"

    ابحث بناءً على المعنى وليس الاسم فقط.
    إذا لم يكن متاحًا الكتاب المطلوب، اقترح عليه كتبًا مشابهة بالموضوع.

    صيغة الرد يجب أن تكون بالعربية الواضحة، وتشمل:
    - اسم الكتاب
    - المؤلف
    - وصف مختصر للكتاب
    - سبب الترشيح بناءً على وصف المستخدم
    """
    try:
        response = client.models.generate_content(
            model="gemini-1.5-flash",
            contents=prompt,
            config=GenerateContentConfig(max_output_tokens=400),
        )
        return response.text.strip()
    except Exception as e:
        return f"حدث خطأ أثناء البحث الذكي عن الكتاب: {e}"


# 📚 اقتراح كتب حسب مجال معين
def ai_suggest_books(field: str) -> str:
    """
    يقترح ٥ كتب بناءً على مجال يحدده المستخدم (مثل: تاريخ، علم نفس، فلسفة، برمجة...)
    """
    prompt = f"""
    اقترح 5 كتب مشهورة في مجال "{field}".
    يجب أن يكون الرد منسقًا هكذا:
    1. اسم الكتاب – المؤلف – وصف مختصر
    2. ...
    اكتب بالعربية فقط.
    """
    try:
        response = client.models.generate_content(
            model="gemini-1.5-flash",
            contents=prompt,
            config=GenerateContentConfig(max_output_tokens=500),
        )
        return response.text.strip()
    except Exception as e:
        return f"حدث خطأ أثناء اقتراح الكتب: {e}"
